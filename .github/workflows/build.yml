# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: Build
on:
  pull_request: {}
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build
        run: ./deployment/build-s3-dist.sh dist-bucket amazon-emr-with-juicefs latest
      - name: Build Benchmark
        run: make -C ./source/benchmark-sample/tpcds-gen

